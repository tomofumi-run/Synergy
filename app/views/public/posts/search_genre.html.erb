<% provide(:title, "ジャンル検索結果")%>
<%= render "public/posts/post_search"%>

<div class="container">
  <h5>検索ジャンル　"<%= @content.name %>"</h5>
  <div class="row">
    <%= render "public/posts/index", posts: @records%>
  </div>
  <div class="d-flex justify-content-center my-3">
    <%= paginate @records %>
  </div>
</div>